exports.mulu = {
    title: '总结归纳', // 必要的
    //path: '/base/',      // 可选的, 标题的跳转链接，应为绝对路径且必须存在
    collapsable: true, // 可选的, 默认值是 true,
    sidebarDepth: 0, // 可选的, 默认值是 1
    children: [{
            title: '线程安全的实现方法', // 必要的
            path: 'thread/summary/线程安全的实现方法.md'
        }
    ]
}