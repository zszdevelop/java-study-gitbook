exports.mulu = {
    title: '数据库连接池Druid',   // 必要的
    //path: '/base/',      // 可选的, 标题的跳转链接，应为绝对路径且必须存在
    collapsable: true, // 可选的, 默认值是 true,
    sidebarDepth: 1,    // 可选的, 默认值是 1
    children: [
        {
            title: 'Druid多数据源配置',   // 必要的
            path: 'dependencies/druid/Druid多数据源配置.md'
        }
    ]
}